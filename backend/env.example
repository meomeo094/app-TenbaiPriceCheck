# Sao chép thành .env (backend). Không commit .env có secret.

PORT=3001

# Supabase — ghi bảng my_inventory (BẮT BUỘC service role — backend không dùng anon để upsert)
SUPABASE_URL=https://xxxx.supabase.co
SUPABASE_SERVICE_ROLE_KEY=
# (Tuỳ chọn, không dùng cho sync inventory)
SUPABASE_ANON_KEY=
