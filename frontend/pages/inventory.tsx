import InventoryScreen from "@/components/InventoryScreen";

/**
 * Trang Pages Router: /inventory (cùng UI với App Router /my-inventory).
 */
export default function InventoryPage() {
  return <InventoryScreen />;
}
